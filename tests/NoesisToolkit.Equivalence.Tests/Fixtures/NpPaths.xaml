<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:t="clr-namespace:NoesisToolkit.Equivalence.Tests;assembly=NoesisToolkit.Equivalence.Tests"
  xmlns:ntk="https://github.com/vrimar/NoesisToolkit"
  ntk:CompileBindings="True"
>
  <t:JoinConverter x:Key="NpJoin" />

  <ControlTemplate x:Key="Paths" TargetType="{x:Type t:SpikeControl}">
    <StackPanel>
      <TextBlock
        x:Name="Direct"
        Text="{Binding (t:Marker.Value), RelativeSource={RelativeSource TemplatedParent}, Mode=OneTime}"
      />
      <TextBlock
        x:Name="Nested"
        Text="{Binding Tag.(t:Marker.Value), RelativeSource={RelativeSource TemplatedParent}, Mode=OneTime}"
      />
      <TextBlock x:Name="Multi">
        <TextBlock.Text>
          <MultiBinding Converter="{StaticResource NpJoin}" Mode="OneTime">
            <Binding Path="(t:Marker.Value)" RelativeSource="{RelativeSource TemplatedParent}" />
          </MultiBinding>
        </TextBlock.Text>
      </TextBlock>
    </StackPanel>
  </ControlTemplate>
</ResourceDictionary>
